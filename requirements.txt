fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
openai>=1.0          # vLLM client (OpenAI-compatible endpoint) — the agent brain
playwright>=1.44     # headed Chromium automation
# After install:  python -m playwright install chromium
